# TODOS

## Autoplan Deferred Items

- Add an explicit PR detail layout contract: hero status, route summary, checks summary, mergeability summary, actions rail.
- Add a state table for `READY`, `BLOCKED`, `FAILING`, `UNKNOWN`, and stale-target state.
- Add parser and validator test coverage for the v1 `ci.yml` schema.
- Add auth boundary specification for future approve/merge actions and CLI adapter flows.
- Add one copy-paste happy-path onboarding example and a TTHW target under 5 minutes.

## Deferred with a written handoff

- git smart-HTTP 인증 설계와 서블릿 배선 — [task-2.127-b-git-authentication.md](task-2.127-b-git-authentication.md). 2.127 의 노출은 `c2b0856` 에서 닫혔고(`denyAll`), 인증 설계는 미뤘다. 등록용 필드가 문서에 있다.
