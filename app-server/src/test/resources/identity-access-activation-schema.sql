DROP TABLE IF EXISTS USER;
CREATE TABLE USER (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  USERNAME VARCHAR(150) NOT NULL,
  EMAIL VARCHAR(254),
  DISPLAY_NAME VARCHAR(255),
  AVATAR_URL VARCHAR(1024),
  AUTHORITY VARCHAR(32) NOT NULL DEFAULT 'USER',
  STATUS VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  LAST_LOGIN_AT TIMESTAMP NULL,
  CREATED_AT TIMESTAMP NOT NULL,
  UPDATED_AT TIMESTAMP NOT NULL,
  CONSTRAINT UK_USERS_USERNAME UNIQUE (USERNAME),
  CONSTRAINT UK_USERS_EMAIL UNIQUE (EMAIL)
);

INSERT INTO USER (ID, USERNAME, EMAIL, DISPLAY_NAME, AVATAR_URL, AUTHORITY, STATUS, LAST_LOGIN_AT, CREATED_AT, UPDATED_AT)
VALUES (1001, 'pending-user', 'pending@example.test', 'Pending User', NULL, 'USER', 'PENDING', NULL, TIMESTAMP '2026-01-01 00:00:00', TIMESTAMP '2026-01-01 00:00:00');

INSERT INTO USER (ID, USERNAME, EMAIL, DISPLAY_NAME, AVATAR_URL, AUTHORITY, STATUS, LAST_LOGIN_AT, CREATED_AT, UPDATED_AT)
VALUES (1002, 'legacy-pending-user', 'legacy@example.test', 'Legacy Pending User', NULL, 'USER', 'PENDING_USERNAME', TIMESTAMP '2025-12-31 23:00:00', TIMESTAMP '2026-01-01 00:00:00', TIMESTAMP '2026-01-01 00:00:00');
