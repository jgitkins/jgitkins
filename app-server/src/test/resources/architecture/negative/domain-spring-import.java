package io.jgitkins.server.collaboration.domain.aggregate;

// Negative-control fixture for task 2.66. Not compiled: it lives under test resources precisely so it
// can contain the violation the scanner must find. If this ever becomes real source, the scanner is
// pointed at the wrong tree.
import org.springframework.stereotype.Service;

@Service
public class DomainSpringImport {
}
