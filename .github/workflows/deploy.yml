name: deploy

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      server: ${{ steps.filter.outputs.server }}
      web: ${{ steps.filter.outputs.web }}
      runner: ${{ steps.filter.outputs.runner }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: filter
        shell: bash
        run: |
          set -euo pipefail

          if [[ -n "${{ github.event.before }}" && "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]]; then
            base_sha="${{ github.event.before }}"
          else
            base_sha="$(git rev-list --max-parents=0 HEAD)"
          fi

          git diff --name-only "${base_sha}" "${GITHUB_SHA}" > changed_files.txt

          mark() {
            local name="$1"
            local pattern="$2"
            if grep -Eq "${pattern}" changed_files.txt; then
              echo "${name}=true" >> "${GITHUB_OUTPUT}"
            else
              echo "${name}=false" >> "${GITHUB_OUTPUT}"
            fi
          }

          mark server '^(server/|\.github/workflows/deploy\.yml$|\.github/workflows/server-release\.yml$)'
          mark web '^(web/|\.github/workflows/deploy\.yml$|\.github/workflows/web-release\.yml$)'
          mark runner '^(runner/|\.github/workflows/deploy\.yml$|\.github/workflows/runner-release\.yml$)'

  server:
    needs: detect
    if: needs.detect.outputs.server == 'true'
    uses: ./.github/workflows/server-release.yml
    secrets: inherit

  web:
    needs: detect
    if: needs.detect.outputs.web == 'true'
    uses: ./.github/workflows/web-release.yml
    secrets: inherit

  runner:
    needs: detect
    if: needs.detect.outputs.runner == 'true'
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner release workflow is not ready yet."
